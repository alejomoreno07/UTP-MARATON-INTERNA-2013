#include <bits/stdc++.h>
using namespace std;
 
int n,q,tempo,x,y,T;
int ans=0;
//vector<int> v;
 
 
 
 
int main(){
    ios_base::sync_with_stdio(false);
    while(cin>>T){
        if(T==0) return 0;
        cout<<ceil(sqrt(T))<<endl;
    }
    return 0;
}
